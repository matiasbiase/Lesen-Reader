# The Python environment: 239 MB that run.sh rebuilds
.venv/
__pycache__/
*.pyc

# Your words, your saved articles and the server log: yours, not the repo's.
# The database creates itself the first time the server starts.
data/

# Your Tailscale address: it belongs to your machine, not to this project.
address.txt

# macOS clutter
.DS_Store
